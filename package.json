{
  "private": true,
  "scripts": {
    "build": "turbo daemon clean && turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.14",
    "eslint": "^8.50.0",
    "turbo": "^1.10.14"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "examples",
  "dependencies": {
    "eslint-config-ethang": "^11.2.1"
  }
}
